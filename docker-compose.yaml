version: '3'

services:
selenium-hub:
image: selenium/hub
ports:
- "4442-4444:4442-4444"
networks:
  -grid
  
  node- chrome:
     image: selenium/node-chrome
     environment:
     -SE_EVENT_BUS_HOST=selenium-hub
     -SE EVENT_BUS_PUBLISH_PORT=4442
     -SE EVENT_BUS_SUBSCIBE_PORT=4443
     networks:
       -grid
       
  node-firefox:
   image: selenium/node-firefox
     environment:
     -SE_EVENT_BUS_HOST=selenium-hub
     -SE EVENT_BUS_PUBLISH_PORT=4442
     -SE EVENT_BUS_SUBSCIBE_PORT=4443
     networks:
       -grid
       